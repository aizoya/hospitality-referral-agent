# Submission closeout — September 13, 2026

Status: REVIEW_REQUIRED. Submission is not certified complete.
Deadline: September 14, 2026, 5:00 PM PDT. Internal target: September 13.

## Verified evidence

| Check | Result |
| --- | --- |
| Audited main commit | `3836f19348d324f071f6ef925fea97c9b5a37cb3` |
| PR #1 | Merged September 10, 2026 (UTC) |
| Public repository | Public, default branch main, MIT detected by GitHub |
| Repository About description | Empty; optional presentation improvement |
| Main CI | [Success](https://github.com/aizoya/hospitality-referral-agent/actions/runs/34431350446) |
| Fresh local validation | `python -m pytest -q`: 15 passed |
| Deterministic CLI smoke | Score 100, HIGH, within 24 hours, approval_required=true, DRAFT_ONLY_NOT_SENT |
| Public demo | HTTP 404; browser says there is no GitHub Pages site; API has_pages=false |
| Deployment diagnosis | [Run](https://github.com/aizoya/hospitality-referral-agent/actions/runs/34431286626) failed at Configure Pages: Create Pages site failed, Resource not accessible by integration. Upload and deployment were skipped. |
| Devpost access | Secure sign-in subsequently succeeded; existing draft inspected, 3/5 steps done |
| AWS live result | Last recorded quota restriction unresolved; no fresh account access or successful inference claimed |

Tests were run after installing the repository requirements in an isolated local virtual environment. No AWS inference was invoked. This validates deterministic behavior, not live model output or mobile browser layout.

## Hosting unblock

Existing merge/release approval remains recorded. No new publication approval is being requested merely to repeat it.

This session's GitHub connector supports repository changes but exposes no Pages settings or workflow-dispatch operation. The local checkout has no authenticated GitHub CLI. Do not broaden workflow permissions or try to bypass the failed integration.

The shortest existing documented path is repository Settings → Pages → Deploy from a branch → main → /docs → Save. Wait for GitHub's build, then verify the published URL and run the synthetic referral in desktop and phone layouts. Do not include the 404 URL in Devpost.

The alternative existing workflow is manual: after Pages is enabled with GitHub Actions as source, run Deploy public judge demo on main. Use one hosting method; do not mix branch publishing and Actions instructions.

## Submission controls to resolve

1. Secure Devpost sign-in succeeded during this audit. Existing draft found; no duplicate entry created. Reuse the current session while available.
2. RESOLVED: founder confirmed AIZOYA is not yet legally formed and explicitly approved Individual. Individual and 'Not applicable — individual entrant' were saved and verified after reopening the form.
3. Confirm the final public YouTube/Vimeo video, maximum five minutes. The recording script exists; a script is not a completed video.
4. Confirm the architecture image is uploaded, Builder ID entered, Professional Agents track selected, required fields complete, and pre-existing work disclosed accurately.
5. Recheck AWS quota in the existing authorized CloudShell context. If usable access returns, run the existing controlled validation once and capture safe output. No IAM broadening, access-key creation, model switch, or paid-capacity change.
6. Prepare the final entry for founder review and retain actual submission confirmation. No final submission is claimed or performed by this audit.

## Important rules correction

[Official rules](https://agentsforhumans.devpost.com/rules), reviewed September 13, require a working Strands project that functions as depicted. The optional live-demo URL and optional AgentCore deployment are distinct from successful agent functionality. The internally approved deterministic fallback does not establish an organizer exception. Retain truthful disclosure and treat the missing end-to-end response as an unresolved functionality/judging risk.

## Evidence-safe description for the current fallback

Hospitality Referral Agent targets referral follow-up for independent hospitality businesses. The verified offline demo scores synthetic referrals using five transparent factors and shows priority, timing, and an owner-review checkpoint. The repository implements a Strands Agent and scoring tool for model-generated explanation and follow-up drafting. That live Strands/Bedrock response has not yet been successfully verified because the last recorded account-level quota restriction blocked invocation. The static demo does not generate drafts or send outreach. No automatic sending capability is implemented.

## Scope and next gate

This closeout changes documentation only. Application behavior, IAM, models, hosting and production are unchanged. No fees incurred by this audit. Resume with Pages enablement, the missing video, and entrant confirmation; preserve September 14 as correction buffer.

## Authenticated draft audit — same session

- Existing Hospitality Referral Agent entry is Draft, with 3/5 steps complete; it has not been submitted.
- Project story and repository link are saved; five gallery images are present (visual content not certified in this audit).
- Required video URL is empty. A recording script is available, but no completed video is established by this audit.
- Additional info contains the Professional Agents track, public repository URL, AWS Builder profile URL, and uploaded `aizoya_hospitality_system_architecture.png`. Upload presence is verified; image legibility/content remains to be reviewed.
- Optional live demo and bonus-blog fields are empty, appropriately avoiding the broken Pages URL.
- Initial audit found Organization / AIZOYA. This was subsequently corrected with explicit founder approval: Individual / Not applicable — individual entrant.
- Finalization page was inspected without checking the terms box or pressing Submit project. Final terms acceptance remains a human decision.
- Draft PR #3 first revision CI passed, run 110. Subsequent changes in this update only reconcile audit documentation.
- Browser returned to Project details, ready for the missing video. No Devpost form values were changed by this audit.

## Continuation — approved entrant and gallery review

The founder confirmed AIZOYA is not yet legally formed, then explicitly approved changing the draft to Individual. The saved form was reopened and verified: Individual selected, organization field 'Not applicable — individual entrant'. Saving an empty organization value initially retained AIZOYA, so the explicit not-applicable text was used. Terms were not accepted and the entry remains a draft.

All five gallery images were visually inspected in the Devpost preview. These are branded concept/illustration graphics, not screenshots proving execution of the current application. The implementation graphic contains a fixed example score of 87 and a HIGH threshold of 70; actual Python and static JavaScript use HIGH >=80 and MEDIUM >=60. The output graphic shows a drafted message that is not verified live Bedrock output. Avoid presenting these graphics as runtime evidence.

### Prepared captions (not yet applied)

| Image | Exact proposed caption |
| --- | --- |
| 1 — hero | Product concept illustration; the implemented offline demo is shown separately. |
| 2 — architecture | Architecture overview. Email, SMS and CRM are future scope; live Bedrock output remains unverified. |
| 3 — implementation | Illustrative pseudocode, not repository code. Actual thresholds: HIGH >=80, MEDIUM >=60. See GitHub for implementation. |
| 4 — output | Concept output with illustrative score and draft. Not a captured live Strands/Bedrock response. |
| 5 — input | Proposed input-screen design; not a screenshot of the implemented demo. |

Preferred final gallery correction: replace image 3 with real source evidence and images 4/5 with real application captures once hosting is working; retain concise concept labels for remaining illustrative assets. No existing images were deleted or modified in this review.

### Recording readiness

- The existing recording package is ready; record actual application interaction rather than animating concept graphics as if they were working screens.
- Static browser warm/high sample scores 96; the CLI hot/high sample scores 100. Different samples explain the difference; do not narrate them as identical input.
- Show real source at src/referral_agent.py, including the 80/60 thresholds and Strands Agent/@tool integration.
- No successful live Bedrock response has been captured in this session.
- A focused read-only HeyGen title search for 'Hospitality' returned no videos. This does not establish that no differently titled video exists. No credit-consuming render was started.

### Immediate manual hosting step

The existing GitHub connection lacks Pages settings and workflow-dispatch tools; a GitHub UI fallback is prohibited by the browser skill when the applicable plugin capability is unavailable. Existing release authorization remains valid. The founder can complete the repository setting directly: Settings → Pages → Deploy from a branch → main → /docs → Save. After GitHub completes the build, resume public desktop/mobile interaction checks, recording, and the final Devpost review.

## Status refresh — September 14, 2026 (UTC)

Fresh GitHub API check: has_pages=false; public demo HTTP 404. PR #3 remains open and draft; audited head before this note was 50ac9dc34d294fba2d8db579a1f72f9abc58b4b2. No new application commits or live AWS success were established. Issue #2 remains at its September 10 update; its fallback language is qualified by this closeout's official-rules correction.

The browser session was reconnected and the existing Devpost draft inspected directly: authenticated, Draft, 3/5 steps, required video URL empty. The previous approved Individual entrant correction remains the last verified identity state; do not revive the resolved entrant decision based on stale portfolio notes. The saved recording package is available on main, but no recorded/published video is established.

Deadline rechecked on official Devpost: September 14, 2026, 5:00 PM PDT. Public hosting is optional and should not block a local working-product recording. Missing video remains a mandatory submission gate; missing live Strands proof remains a functionality/judging risk. No final submission or terms acceptance performed. No paid video render initiated.

Immediate continuation: owner enables existing GitHub Pages setting when available; record the implemented local or public workflow using docs/video-recording-package.md; supply the public YouTube/Vimeo URL for duration/content verification and final form completion. Final entry review must also resolve the prepared gallery captions/replacements, then capture actual submission confirmation before the cutoff.
